import styled from "styled-components"

export const RecomendacaoWrapper = styled.div`
    color: var(--text-color);
    text-align: center;
`

export const RecomendacaoTitle = styled.h1`
    font-size: 1.5rem;
    font-weight: bold;
    line-height: 1.8rem;
    margin-bottom: 20px;

    span {
        color: var(--primary-color);
        text-transform: uppercase;
    }
`
export const RecomendacaoSubtitle = styled.h2`
    font-style: italic;
    line-height: 1.25rem;
    margin: 20px 0;
`